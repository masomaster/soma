# SQL migrations

Numbered files applied in order:

- `0001_description.sql`, `0002_...sql`, etc.

**Phase 1** will add the first migration aligned with [../soma-planned-schema.sql](../soma-planned-schema.sql). Until then the planned DDL lives only in that file for review and Bruno/Supabase manual apply if needed.
